name: CI/CD Pipeline

on:
  push:
    branches: [ master, main, develop ]
    tags:
      - 'v*'
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'doc_new/**'
      - '.vscode/**'
      - '.idea/**'
      - 'LICENSE'
  pull_request:
    branches: [ master, main, develop ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'doc_new/**'
      - '.vscode/**'
      - '.idea/**'
      - 'LICENSE'

permissions:
  contents: read
  checks: write

env:
  FIREBASE_PROJECT_ID: easypool-30af3

jobs:
  # ============================================================================
  # DECISION HEAD: Routes deployment (path shown in summary)
  # ============================================================================
  decide:
    name: 🎯 Decide
    runs-on: ubuntu-latest
    outputs:
      should_deploy: ${{ steps.route.outputs.should_deploy }}
      deploy_develop: ${{ steps.route.outputs.deploy_develop }}
      deploy_staging: ${{ steps.route.outputs.deploy_staging }}
      deploy_production: ${{ steps.route.outputs.deploy_production }}
      path_indicator: ${{ steps.route.outputs.path_indicator }}
    steps:
      - name: Route workflow
        id: route
        run: |
          # Initialize
          SHOULD_DEPLOY=false
          DEPLOY_DEVELOP=false
          DEPLOY_STAGING=false
          DEPLOY_PRODUCTION=false
          PATH_INDICATOR=""

          # PR: validate only
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            PATH_INDICATOR="🔍 PR VALIDATION"
            SHOULD_DEPLOY=false
            echo "# 🎯 CHOSEN PATH: PR VALIDATION" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "## 🔍 PR VALIDATION" >> $GITHUB_STEP_SUMMARY
            echo "Quality checks + build only, no deployment" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Branch:** ${{ github.head_ref }}" >> $GITHUB_STEP_SUMMARY
            echo "**Action:** Validate only (no deployment)" >> $GITHUB_STEP_SUMMARY

          # develop: deploy to develop site
          elif [[ "${{ github.ref }}" == "refs/heads/develop" ]]; then
            PATH_INDICATOR="🟡 DEVELOP → dev.easypool.in"
            SHOULD_DEPLOY=true
            DEPLOY_DEVELOP=true
            echo "# 🎯 CHOSEN PATH: DEVELOP DEPLOYMENT" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "## 🟡 DEVELOP → dev.easypool.in" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Branch:** develop" >> $GITHUB_STEP_SUMMARY
            echo "**Target:** Development environment" >> $GITHUB_STEP_SUMMARY
            echo "**URL:** https://dev.easypool.in" >> $GITHUB_STEP_SUMMARY

          # master: deploy to staging site
          elif [[ "${{ github.ref }}" == "refs/heads/master" ]] || [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            PATH_INDICATOR="🟠 STAGING → stage.easypool.in"
            SHOULD_DEPLOY=true
            DEPLOY_STAGING=true
            echo "# 🎯 CHOSEN PATH: STAGING DEPLOYMENT" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "## 🟠 STAGING → stage.easypool.in" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Branch:** master/main" >> $GITHUB_STEP_SUMMARY
            echo "**Target:** Staging environment" >> $GITHUB_STEP_SUMMARY
            echo "**URL:** https://stage.easypool.in" >> $GITHUB_STEP_SUMMARY

          # v* tag: deploy to production
          elif [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            PATH_INDICATOR="🔴 PRODUCTION → easypool.in"
            SHOULD_DEPLOY=true
            DEPLOY_PRODUCTION=true
            echo "# 🎯 CHOSEN PATH: PRODUCTION DEPLOYMENT" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "## 🔴 PRODUCTION → easypool.in" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Tag:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
            echo "**Target:** Production environment" >> $GITHUB_STEP_SUMMARY
            echo "**URL:** https://easypool.in" >> $GITHUB_STEP_SUMMARY
          fi

          # Set outputs
          echo "should_deploy=$SHOULD_DEPLOY" >> $GITHUB_OUTPUT
          echo "deploy_develop=$DEPLOY_DEVELOP" >> $GITHUB_OUTPUT
          echo "deploy_staging=$DEPLOY_STAGING" >> $GITHUB_OUTPUT
          echo "deploy_production=$DEPLOY_PRODUCTION" >> $GITHUB_OUTPUT
          echo "path_indicator=$PATH_INDICATOR" >> $GITHUB_OUTPUT

  # ============================================================================
  # QUALITY: Parallel quality checks (blocking for deployment)
  # ============================================================================
  quality:
    name: ⚡ Quality
    runs-on: ubuntu-latest
    needs: [decide]
    if: needs.decide.outputs.should_deploy == 'true' || github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-flutter
      - uses: ./.github/actions/quality-check
        with:
          fail-on-warnings: 'false'

  # ============================================================================
  # BUILD: Single build job
  # ============================================================================
  build:
    name: 🏗️ Build
    runs-on: ubuntu-latest
    needs: [decide]
    if: needs.decide.outputs.should_deploy == 'true' || github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-flutter
      - uses: ./.github/actions/build-web

  # ============================================================================
  # DEPLOY: Only the matching environment shows (others hidden)
  # ============================================================================

  deploy-develop:
    name: 🟡 Deploy to Develop
    runs-on: ubuntu-latest
    needs: [decide, quality, build]
    if: needs.decide.outputs.deploy_develop == 'true'
    environment:
      name: develop
      url: https://dev.easypool.in
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: 'projects/725567398741/locations/global/workloadIdentityPools/github-pool/providers/github-provider-frontend'
          service_account: 'firebase-deployer@easypool-30af3.iam.gserviceaccount.com'

      - uses: ./.github/actions/deploy-firebase
        with:
          site: easypool-develop
          target: develop
          project-id: ${{ env.FIREBASE_PROJECT_ID }}
          build-artifact: web-build-${{ github.sha }}

      - run: |
          echo "# 🟡 DEVELOP DEPLOYED" >> $GITHUB_STEP_SUMMARY
          echo "**URL:** https://dev.easypool.in" >> $GITHUB_STEP_SUMMARY

  deploy-staging:
    name: 🟠 Deploy to Staging
    runs-on: ubuntu-latest
    needs: [decide, quality, build]
    if: needs.decide.outputs.deploy_staging == 'true'
    environment:
      name: staging
      url: https://stage.easypool.in
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: 'projects/725567398741/locations/global/workloadIdentityPools/github-pool/providers/github-provider-frontend'
          service_account: 'firebase-deployer@easypool-30af3.iam.gserviceaccount.com'

      - uses: ./.github/actions/deploy-firebase
        with:
          site: easypool-staging
          target: staging
          project-id: ${{ env.FIREBASE_PROJECT_ID }}
          build-artifact: web-build-${{ github.sha }}

      - run: |
          echo "# 🟠 STAGING DEPLOYED" >> $GITHUB_STEP_SUMMARY
          echo "**URL:** https://stage.easypool.in" >> $GITHUB_STEP_SUMMARY

  deploy-production:
    name: 🔴 Deploy to Production
    runs-on: ubuntu-latest
    needs: [decide, quality, build]
    if: needs.decide.outputs.deploy_production == 'true'
    environment:
      name: production
      url: https://easypool.in
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: 'projects/725567398741/locations/global/workloadIdentityPools/github-pool/providers/github-provider-frontend'
          service_account: 'firebase-deployer@easypool-30af3.iam.gserviceaccount.com'

      - uses: ./.github/actions/deploy-firebase
        with:
          site: easypool-30af3
          target: live
          project-id: ${{ env.FIREBASE_PROJECT_ID }}
          build-artifact: web-build-${{ github.sha }}

      - run: |
          echo "# 🔴 PRODUCTION DEPLOYED" >> $GITHUB_STEP_SUMMARY
          echo "**URL:** https://easypool.in" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
