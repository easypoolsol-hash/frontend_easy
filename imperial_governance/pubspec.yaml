name: imperial_governance
description: Constitutional enforcement system for Frontend Easy - Architecture as Code
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  # Dart analyzer for AST-based code detection (2025 latest)
  analyzer: ^8.4.0

  # YAML parsing for constitutional contracts
  yaml: ^3.1.2

  # Path utilities
  path: ^1.9.1

dev_dependencies:
  lints: ^5.1.1
