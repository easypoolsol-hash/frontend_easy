name: frontend_easy_api
version: 1.0.0
description: OpenAPI API client
homepage: homepage


environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  dio: 5.7.0  # Pinned for stability (Google best practice)
  one_of: 1.5.0  # Pinned for stability
  one_of_serializer: 1.5.0  # Pinned for stability
  built_value: 8.10.1  # Required by riverpod_generator ^3.0.0
  built_collection: 5.1.1  # Pinned for stability

dev_dependencies:
  built_value_generator: 8.10.1  # Must match built_value version
  build_runner: 2.4.13  # Pinned for stability
  test: 1.16.0  # Pinned for stability
